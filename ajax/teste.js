// const bp = require('body-parser');
// const eps = require('express');


// eps().use(bp.static('.'));
// eps().use(bp.urlencoded({ extended: true }));
// eps().use(bp.json);

// eps().get('/teste', (requisicao, response) => response.send('OK'));

// eps().listen(8000, () => )